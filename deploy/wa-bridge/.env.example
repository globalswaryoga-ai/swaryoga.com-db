# Used by deploy/wa-bridge/docker-compose.yml

# Allow browser requests from your CRM domains
WHATSAPP_WEB_ALLOWED_ORIGINS=https://crm.swaryoga.com,https://swaryoga.com,https://www.swaryoga.com

# Optional: where to POST inbound messages into the Next.js app
NEXT_BASE_URL=https://crm.swaryoga.com

# Optional: shared secret required by /api/admin/crm/whatsapp/inbound
# Must match the secret configured in your Next.js env.
WHATSAPP_WEB_BRIDGE_SECRET=replace-with-strong-random

# Optional: if you want separate sessions per deployment
WHATSAPP_CLIENT_ID=crm-whatsapp-session
