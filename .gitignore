node_modules
.next/
out/
.turbo/
.swc/

# TypeScript incremental build info (local build artifact)
*.tsbuildinfo
# Documentation files
*.md
!README.md
!docs/QR_WHATSAPP_ACCEPTANCE_PLAYBOOK.md

# Environment files
.env
.env.*
!.env.example
!.env.*.example
!.env.*.template

# Local-only / experimental
lib/payments/payu-v2.ts
test-payment-opening.js

# Test / coverage
coverage/

# WhatsApp Web (wwebjs) session/cache (never commit)
.wwebjs_auth/
.wwebjs_cache/
**/.wwebjs_auth/
**/.wwebjs_cache/

# Ignore markdown documentation files (generated/docs)
# NOTE: We commit project docs (README, deployment guides, etc.).
# Only ignore generated markdown (if any) under known generated folders.
docs/generated/**/*.md

# Environment files
.env
.env.local
.env.production
.env.*.local
.env.vercel

# Vercel CLI token files (never commit)
production

.vercel
.env*.local
