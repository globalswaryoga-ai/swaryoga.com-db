# WhatsApp Web Bridge (INTERNAL / TESTING ONLY)
PORT=4010
WHATSAPP_CLIENT_ID=crm-whatsapp-session

# Optional: protect API with a shared secret header.
# Client must send: x-api-key: <value>
WHATSAPP_WEB_BRIDGE_SECRET=change-me
